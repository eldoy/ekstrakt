{
  "name": "ekstrakt",
  "version": "0.2.0",
  "description": "Extract things from strings",
  "main": "index.js",
  "scripts": {
    "test": "nodemon -q --exec spekk"
  },
  "license": "ISC",
  "dependencies": {
    "emoji-regex": "^9.2.2"
  }
}
